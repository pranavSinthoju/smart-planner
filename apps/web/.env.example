# Only needed in production (Vercel). Leave unset for local dev — Vite's
# dev-server proxy (vite.config.ts) handles /api/* against localhost:4000.
# VITE_API_URL="https://<your-railway-app>.up.railway.app"
